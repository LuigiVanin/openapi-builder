version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    # - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    # - errorlint

  settings:
    staticcheck:
      checks:
        - all
        - -ST1006
        - -ST1003
